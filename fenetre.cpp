#include "fenetre.h"
#include <QLabel>
#include<QLineEdit>



/*fenetre::fenetre() : QWidget(){
    setFixedSize(1000, 700);

    // Construction du bouton
    b_quitter = new QPushButton("quitter",this);

    b_quitter->setFont(QFont("Comic Sans MS", 14));
    b_quitter->setCursor(Qt::PointingHandCursor);
    b_quitter->move(900, 600);
    QObject::connect(b_quitter, SIGNAL(clicked()), qApp, SLOT(quit()));

    QLabel* num1 = new QLabel("Numéro", this);
    QLabel* num2 = new QLabel("NuméroBit", this);
    num1->move(10, 10);
    num2->move(10, 45);

    QLineEdit* num = new QLineEdit(this);
    num->setFixedWidth(25);
    num-> move(80, 10);
    QLineEdit* numeroBit = new QLineEdit(this);
    numeroBit->setFixedWidth(60);
    numeroBit-> move(80, 45);

}

*/
